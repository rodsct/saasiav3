# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# Copy this file to .env and update the values for your production server
# =============================================================================

# Database Configuration
DATABASE_URL="postgresql://saasiav3:saasiav3_password@postgres:5432/saasiav3"

# NextAuth Configuration (REQUIRED - CHANGE THESE!)
NEXTAUTH_URL="https://tu-dominio.com"
NEXTAUTH_SECRET="genera-un-secreto-super-largo-y-seguro-de-al-menos-32-caracteres"
SECRET="genera-un-secreto-super-largo-y-seguro-de-al-menos-32-caracteres"

# Site Configuration
NEXT_PUBLIC_SITE_URL="https://tu-dominio.com"
NODE_ENV="production"

# n8n Webhook Configuration
DEFAULT_N8N_WEBHOOK="https://infra-v2-n8n-v2.uclxiv.easypanel.host/webhook/saasiav3"

# Google OAuth (Optional - for Google login)
GOOGLE_CLIENT_ID="tu-google-client-id"
GOOGLE_CLIENT_SECRET="tu-google-client-secret"

# GitHub OAuth (Optional - for GitHub login)  
GITHUB_CLIENT_ID="tu-github-client-id"
GITHUB_CLIENT_SECRET="tu-github-client-secret"

# Email Configuration (Optional - for email login)
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="tu-email@gmail.com"
EMAIL_SERVER_PASSWORD="tu-password-de-app"
EMAIL_FROM="noreply@tu-dominio.com"

# Stripe Configuration (Optional - for payments)
STRIPE_PUBLIC_KEY="pk_live_..."
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# =============================================================================
# INSTRUCTIONS:
# 1. Replace "tu-dominio.com" with your actual domain
# 2. Generate secure secrets: openssl rand -base64 32
# 3. Configure OAuth apps in Google/GitHub consoles
# 4. Set up email provider if using email auth
# 5. Configure Stripe if using payments
# =============================================================================